{% extends "templates/headerFooterSearch.twig" %}

{% block title %}Stagehub - Liste des étudiants{% endblock %}

{% block filter %}

    <div class="flex-col">
        <!-- Sidebar -->
        <div class="ml-0 mt-0 bg-gray-200 p-4">
            <!-- Filter options -->
            <h2 class="text-lg font-semibold mb-1">Affinez votre recherche</h2>
            <div class="space-y-2">
                <!-- Example filter options -->
                <div>
                    <label class="block uppercase tracking-wide text-gray-700 text-xs font-bold mb-2" for="category">
                        Centre
                    </label>
                    <select id="category" name="category" class="block w-full px-3 py-2 border border-gray-300 bg-white rounded-md shadow-sm focus:outline-none focus:border-indigo-500">
                        <option value=""> Votre centre ?</option>
                        <option value="1">Reims</option>
                        <option value="2">Paris</option>
                        <option value="3">Nancy</option>
                    </select>
                </div>
                <!-- Add more filter options here -->
                <div>
                    <label class="block uppercase tracking-wide text-gray-700 text-xs font-bold mb-2" for="category">
                        Promotion
                    </label>
                    <select id="category" name="category" class="block w-full px-3 py-2 border border-gray-300 bg-white rounded-md shadow-sm focus:outline-none focus:border-indigo-500">
                        <option value="">Quelle promotion ?</option>
                        <option value="1">A1</option>
                        <option value="2">A2</option>
                        <option value="3">A3</option>
                        <option value="3">A4</option>
                        <option value="3">A5</option>
                    </select>

                </div>
            </div>
        </div>
    </div>
{% endblock %}

{% block content %}
    <style>
        /* Ajoutez ce style pour mettre en surbrillance la ligne sélectionnée */
        .selected {
            background-color: rgba(156, 163, 175);
        }
    </style>
    <script>
        function handleRowClick(event) {
            const row = event.currentTarget;
            const id = row.dataset.id;

            // Supprimer la classe "selected" de toutes les lignes
            const rows = document.querySelectorAll("tr.selected");
            rows.forEach((r) => r.classList.remove("selected"));

            // Ajouter la classe "selected" à la ligne actuelle
            row.classList.add("selected");

            // Gérez la sélection ici en utilisant l'identifiant (id)
            console.log(`La ligne ${id} a été sélectionnée.`);
        }
    </script>
</head>
<body>
<div class="container mx-auto px-4 py-8">
    <h2 class="text-2xl font-semibold text-gray-800 mb-6">Liste des étudiants</h2>
    <table class="w-full text-left table-auto border-collapse">
        <thead>
        <tr class="bg-gray-100">
            <th class="px-4 py-2 border">Nom</th>
            <th class="px-4 py-2 border">Prénom</th>
            <th class="px-4 py-2 border">Centre</th>
            <th class="px-4 py-2 border">Promotion</th>
        </tr>
        </thead>
        <tbody>

        {% for user in data %}
            <tr class="border-b even:bg-gray-50 odd:bg-white cursor-pointer" data-id="1" onclick="handleRowClick(event)">
                <td class="px-4 py-2 border">{{ user[0] }}</td>
                <td class="px-4 py-2 border">{{ user[1] }}</td>
                <td class="px-4 py-2 border">{{ user[2] }}</td>
                <td class="px-4 py-2 border">{{ user[3] }}</td>
            </tr>
        {% endfor %}

        </tbody>
    </table>
    <div class="flex justify-center space-x-4 mt-8">
        <a href="/creationStudents" class="bg-blue-500 hover:bg-blue-700 text-white font-semibold py-2 px-4 rounded focus:outline-none focus:shadow-outline" type="button">Créer un compte étudiant</a>
        <a href="#" class="bg-yellow-500 hover:bg-yellow-700 text-white font-semibold py-2 px-4 rounded focus:outline-none focus:shadow-outline" type="button">Modifier un compte étudiant</a>
        <a href="#" class="bg-red-500 hover:bg-red-700 text-white font-semibold py-2 px-4 rounded focus:outline-none focus:shadow-outline" type="button">Supprimer un compte étudiant</a>
        <a href="/statsStudents" class="bg-green-500 hover:bg-green-700 text-white font-semibold py-2 px-4 rounded focus:outline-none focus:shadow-outline" type="button">Stats de l'étudiant</a>
    </div>
</div>

{% endblock %}
