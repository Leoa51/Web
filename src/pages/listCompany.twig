{% extends 'templates/headerFooterSearch.twig' %}

{% block title %}Stagehub - Liste des entreprises{% endblock %}

{% block filter %}
    <form id="filterForm">
        <input type="text" id="searchKeyword" name="search" placeholder=" domaine de stage ...">
        <select id="localisation" name="localisation">
            <option value="">Choisir une localisation</option>
            <option value="Reims">Reims</option>
            <option value="Reims">nancy</option>
            <option value="Reims">stragboug</option>
            <option value="Reims">paris</option>


        </select>
        <select id="localisation" name="promotion ">
            <option value=""> secteur d'activité  </option>
            <option value="Reims">Technologie et informatique </option>
            <option value="Reims">industrie</option>
            <option value="Reims">transport et logistique </option>
            <option value="Reims">meszdias et divertissements</option>
            <option value="Reims"> finance et asssurance</option>

        </select>
        <!-- Ajoutez d'autres filtres ici -->
        <button type="submit" id="applyFilters" class="bg-blue-500 hover:bg-blue-700 text-white font-bold py-4 px-4 rounded">
            Appliquer les filtres
        </button>
    </form>
    <div class="flex-col">
        <!-- Sidebar -->
        <div class="ml-0 mt-0 bg-gray-200 p-4">
            <!-- Filter options -->

            <div class="space-y-2">
                <!-- Example filter options -->

                <!-- Add more filter options here -->
                <div>
                    <label class="block uppercase tracking-wide text-gray-700 text-xs font-bold mb-2" for="category">

                    </label>


                </div>
            </div>
        </div>
    </div>
{% endblock %}

{% block content %}
    <style>
        /* Ajoutez ce style pour mettre en surbrillance la ligne sélectionnée */
        .selected {
            background-color: rgba(156, 163, 175);
        }
    </style>
    <script>
        function handleRowClick(event) {
            const row = event.currentTarget;
            const id = row.dataset.id;

            // Supprimer la classe "selected" de toutes les lignes
            const rows = document.querySelectorAll("tr.selected");
            rows.forEach((r) => r.classList.remove("selected"));

            // Ajouter la classe "selected" à la ligne actuelle
            row.classList.add("selected");

            // Gérez la sélection ici en utilisant l'identifiant (id)
            console.log(`La ligne ${id} a été sélectionnée.`);
        }
    </script>
    <body>
    <div class="container mx-auto px-4 py-8">
        <h2 class="text-2xl font-semibold text-gray-800 mb-6">Liste des entreprises</h2>
        <table class="w-full text-left table-auto border-collapse">
            <thead>
            <tr class="bg-gray-100">
                <th class="px-4 py-2 border">Nom de l'entreprise</th>
                <th class="px-4 py-2 border">secteur d'activité </th>
                <th class="px-4 py-2 border">Localisation</th>
            </tr>
            </thead>
            <tbody>
            {% for key,user in data %}
                <tr class="border-b even:bg-gray-50 odd:bg-white cursor-pointer" data-id="1" onclick="handleRowClick(event)">
                    <td class="px-4 py-2 border">{{ user[0] }}</td>
                    <td class="px-4 py-2 border">{{ user[1] }}</td>
                    <td class="px-4 py-2 border">{{ user[2] }}</td>
                    <td class="px-4 py-2 border">{{ user[3] }}</td>
                </tr>
            {% endfor %}
            </tbody>
        </table>
        <div class="flex justify-center space-x-4 mt-8">
            <a href="/publishCompany" class="bg-blue-500 hover:bg-blue-700 text-white font-semibold py-2 px-4 rounded focus:outline-none focus:shadow-outline" type="button">Ajouter une entreprise</a>
            <a href="#" class="bg-yellow-500 hover:bg-yellow-700 text-white font-semibold py-2 px-4 rounded focus:outline-none focus:shadow-outline" type="button">Modifier une entreprise</a>
            <a href="#" class="bg-red-500 hover:bg-red-700 text-white font-semibold py-2 px-4 rounded focus:outline-none focus:shadow-outline" type="button">Supprimer une entreprise</a>
        </div>
    </div>

{% endblock %}